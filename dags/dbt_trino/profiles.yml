lakehouse_analytics:
  outputs:
    trino_output:
      type: trino
      method: basic
      user: "{{ env_var('TRINO_USERNAME', 'trino') }}"
      password: "{{ env_var('TRINO_PASSWORD') }}"
      database: iceberg
      host: lakehouse-lakekit-trino.lakehouse.svc.cluster.local
      port: 8080
      schema: staging
      threads: 1
      http_scheme: http
  target: trino_output
